# 忽略规则

# ---------------------------------------------------------------
# 注意：dist/ 故意**不**被忽略。
#
# .lcp 是分发包，入库才能让 jsDelivr 直接代理（它只能代理仓库内容，不能代理
# Release 附件），也才能让任何人 git checkout 某个 tag 逐字节核对线上包。
# 详见 README.md「为什么同时提交源码与 .lcp」。
# ---------------------------------------------------------------

# 操作系统噪声
.DS_Store
Thumbs.db
desktop.ini

# 编辑器
.vscode/
.idea/
*.swp

# 构建脚本的依赖（当前没有依赖，保留以防将来引入）
node_modules/

# 压缩过程中的临时文件
*.zip.tmp
