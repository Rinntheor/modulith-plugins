# 贡献指南

## 1. 现阶段只接受维护者自己的插件

这不是客套话，是一个**技术前提**，理由是权限模型目前是**声明式**的。

插件的代码与宿主界面运行在同一个 JavaScript 上下文里，能接触到宿主暴露的一切。清单里的
`permissions` 能告知用户、能作为准入依据，但**拦不住恶意插件**：一个声明「我只读剪贴板」
的插件，代码里照样可以调用宿主提供的任何接口。

要真正隔离，需要把插件放进 Web Worker、iframe 或独立进程，通过消息通道通信。那意味着
一整套宿主 API 的 RPC 协议，且现有插件全部需要改造。

**在只有维护者自己的代码时做这套隔离，是为一个尚不存在的第三方生态付出重构全部插件的
代价。** 因此顺序是：先把自己的插件跑通、把分发链路跑通，等真的收到投稿请求时再做沙箱。

投稿开放时会更新本文，并说明审查标准。维护者一侧的完整流程（审核清单、合并后重新生成并签发索引、打 tag、上架验证）已经写在 [docs/审核与合并.md](docs/审核与合并.md) —— 它在投稿开放之前就有用：**你自己的插件也要走同一条路**，而那套流程里「在 PR 分支上签名」与「合并后忘了重新生成索引」是两个最容易出错的地方。

## 2. 任何插件的硬性要求

即使只收自己的插件，以下要求现在也成立 —— 它们是把关标准，将来也是：

| 要求 | 理由 |
| --- | --- |
| `permissions` 只写真正用到的 | 权限列表是给用户看风险用的，多写一项就多一分误导 |
| 权限名不得使用冒号写法 | `filesystem:read` 会让整份清单反序列化失败，安装被拒 |
| 样式不得使用 Tailwind 工具类 | Tailwind 在构建宿主时生成类名，插件运行时注入的类名不会被生成 |
| 不提交 `node_modules` 或构建缓存 | 插件是单文件 IIFE，没有运行时依赖 |
| 目录名与插件 ID 未被占用 | ID 同时是安装目录名与数据命名空间，冲突无法调和 |
| 声明 `engines.loopcore` 且只写下界 | 避免宿主升到 2.0 时给所有插件挂一条无价值的兼容性提示 |
| 附 `README.md` 与 `LICENSE` | 前者面向装了插件的人，后者回答「我能不能用」 |

完整规则与理由见 [docs/目录规范.md](docs/目录规范.md)。

## 3. 插件 ID 一旦发布不可更改

插件 ID 决定三件事：安装目录名、数据存储命名空间、以及索引里对它的引用。改名会让
老用户的插件数据失去归属，也会让已安装的旧版本与新版本变成两个不同的插件。

因此 ID 在第一次发布前就要定好，发布后只能新增插件，不能改旧插件的 ID。

## 4. 提交规范

提交信息使用 Conventional Commits，与[应用仓库](https://github.com/Rinntheor/modulith-desktop)
保持一致：

```
feat(hello): 增加重置按钮
fix(hello): 首次打开时闪出一个错误的 0
docs: 补充索引格式的说明
chore(release): hello 升至 1.0.1
```

一个提交只做一件事。改动某个插件的代码、又改了它的清单版本，应当分成两个提交：前者是
行为变化，后者是发布动作。

## 5. 版本号

插件版本号遵循语义化版本，写在 `manifest.json` 的 `version` 里，**必须与 tag 中的版本一致**。

| 变化 | 版本位 |
| --- | --- |
| 修 bug、改文案、调整样式 | PATCH |
| 新增功能，且向后兼容 | MINOR |
| 改变已有行为、改数据格式、加新权限 | MAJOR |

**新增权限属于 MAJOR。** 权限变化会触发用户侧的确认流程（新增中高风险权限需要用户
明确同意），把它当成小版本会让升级在用户那里显得突然。

**升版本的判据是「包的内容变了」，不是「我手改了哪个文件」。** `README.md`、`LICENSE`
与 `index.js` 一样都在 `.lcp` 里；从 `src/` 构建的插件，改 `src/` 下的源码同样等于改了
线上包。这些都要升版本 —— `node scripts/build.ts` 会在落盘前直接拒绝「同一个版本号
打包出不同字节」，不给你绕过去的机会。

## 6. 提交前自查

- [ ] `manifest.json` 的 `version` 与即将打的 tag 一致
- [ ] 声明的权限与代码里实际调用的宿主接口一致
- [ ] 已从当前源码重新打包 `.lcp`，而不是沿用上一次的产物
- [ ] 有 `src/` 的插件：`node scripts/build.ts --check` 通过（产物与源码同步），
      且 `npm run typecheck` 通过
- [ ] 已从插件包实际安装一次，确认能加载、能用、重开后状态还在
- [ ] 新插件的 ID 没有被占用

最后两条是同一条：**打包成功不等于插件能装。** 改了源码忘了重新打包，会导致线上包与
仓库源码不一致，而两者看起来都没问题。
